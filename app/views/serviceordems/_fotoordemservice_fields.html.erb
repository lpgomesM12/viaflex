<div class='nested-fields'>
 <div class="row">
  <%if f.object.new_record? %>
        <div class="col-lg-6" >
            <%= f.file_field :foto, :class => "btn btn-default btn-sm"%>
         </div>
   <%end%>

  <% unless f.object.new_record? %>
    <div class="col-lg-6" >
        <%= link_to image_tag(f.object.foto.url(:thumb)),
        f.object.foto.url(:original)%>
    </div>
  <%end%>

    <div class="col-lg-2">
     <%= link_to_remove_association "Deletar", f, :class => "btn btn-danger btn-sm" %>
    </div>
   </div>
</div>
